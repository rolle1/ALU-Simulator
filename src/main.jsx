import React from 'react'
import ReactDOM from 'react-dom/client'
import ALUSimulator from './alu-simulator'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ALUSimulator />
  </React.StrictMode>
)
